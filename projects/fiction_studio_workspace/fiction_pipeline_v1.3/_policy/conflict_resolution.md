# Conflict Resolution

Conflicts must be explicit.

## Default priority

1. deny wins
2. room policy overrides weaker defaults
3. role policy applies inside the room
4. parent window constraints carry forward
5. manual override requires approval

## Required behavior

- do not silently widen permissions
- do not silently change canon
- do not hide unresolved conflicts

