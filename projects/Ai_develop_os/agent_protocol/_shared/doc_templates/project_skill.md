# 项目专属技能库

> 本文件记录与**本项目特定架构、业务规则、模块依赖**相关的经验、踩坑和最佳实践。
> AI 在每次任务后，应评估并将新增知识按以下结构追加到对应章节。

## 1. 架构约定
- [例：本项目采用前后端分离，API 路径统一以 `/api/v1/` 开头]
- [例：所有数据库操作必须通过 `db/sql/` 下的存储过程进行]

## 2. 业务规则
- [例：用户积分计算逻辑在 `src/services/points.ts` 中，不要在其他地方重复实现]
- [例：订单状态流转必须遵循 `PENDING -> CONFIRMED -> SHIPPED -> COMPLETED` 顺序]

## 3. 模块依赖关系
- `模块A` 依赖于 `模块B` 的 [具体接口/函数]
- [记录容易引起循环依赖的模块组合]

## 4. 常见踩坑记录
### [坑1标题]
- **现象**：[描述]
- **原因**：[分析]
- **解决方案**：[方法]
- **相关模块**：[路径]

### [坑2标题]
- **现象**：[描述]
- **原因**：[分析]
- **解决方案**：[方法]

## 5. 配置与环境相关
- [本地开发需要特别注意的环境变量、hosts配置等]
