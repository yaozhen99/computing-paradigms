# vault-cli 本地文件加密命令行工具 — 产品需求书

**创建时间**：2026-05-03 23:00
**状态**：已确认

---

## 1. 需求来源

项目发起人提出开发一个本地文件加密命令行工具 vault-cli，解决个人用户在本地对敏感文件进行加密存储的需求。核心诉求：简单命令、离线可用、安全可靠、零外部服务依赖。

相关引用：`C:\tower-of-babel\projects\vault-cli\_system\user_input.md`

---

## 2. 功能描述

### 2.1 User Story 列表

#### US-01：初始化密钥库

作为**用户**，我希望通过 `vault init` 命令初始化本地密钥库，以便后续的加密和解密操作有可用的密钥派生基础。

**验收标准**：
- [ ] 执行 `vault init` 后，在当前用户目录下生成 `.vault` 目录
- [ ] `.vault` 目录中包含盐值文件（随机生成，至少 16 字节）
- [ ] `.vault` 目录中包含配置文件（记录算法参数：AES-256-GCM、PBKDF2-SHA256、迭代次数等）
- [ ] 若 `.vault` 目录已存在，命令应提示已初始化，不覆盖现有数据
- [ ] 盐值和配置文件的权限应仅限当前用户可读写（Linux 下 600）

---

#### US-02：加密文件

作为**用户**，我希望通过 `vault lock <file>` 命令加密指定文件，以便该文件的内容得到保护，只有通过口令才能还原。

**验收标准**：
- [ ] 输入文件路径后，命令提示输入口令（终端不回显）
- [ ] 使用 PBKDF2-SHA256 从口令 + 盐值派生 256 位密钥
- [ ] 使用 AES-256-GCM 对文件内容进行加密，生成 IV + 密文 + 认证标签
- [ ] 在原文件同目录下生成 `<原文件名>.vault` 文件
- [ ] `.vault` 文件格式包含：版本号头 + IV + 认证标签 + 密文，格式可解析
- [ ] 支持通过 `--keep` 参数保留原文件；默认行为为删除原文件
- [ ] 若文件不存在，输出明确的错误信息并以非零退出码退出
- [ ] 若 `.vault` 未初始化，提示用户先执行 `vault init` 并以非零退出码退出

---

#### US-03：解密文件

作为**用户**，我希望通过 `vault unlock <file.vault>` 命令解密 `.vault` 文件，以便还原原始文件内容。

**验收标准**：
- [ ] 输入 `.vault` 文件路径后，命令提示输入口令（终端不回显）
- [ ] 使用 PBKDF2-SHA256 从口令 + 盐值派生密钥
- [ ] 使用 AES-256-GCM 解密，校验认证标签
- [ ] 认证标签校验失败时（口令错误或文件篡改），输出明确错误信息，不产生任何输出文件，以非零退出码退出
- [ ] 在 `.vault` 文件同目录下还原原始文件（去除 `.vault` 后缀）
- [ ] 支持 `--keep` 参数保留 `.vault` 文件；默认行为为删除 `.vault` 文件
- [ ] 若文件不存在或不是 `.vault` 后缀，输出明确错误并以非零退出码退出
- [ ] 若 `.vault` 未初始化，提示用户先执行 `vault init` 并以非零退出码退出

---

#### US-04：列出加密文件

作为**用户**，我希望通过 `vault list` 命令列出当前目录下所有 `.vault` 加密文件及其状态，以便了解哪些文件已被加密。

**验收标准**：
- [ ] 扫描当前工作目录，列出所有 `.vault` 后缀的文件
- [ ] 对每个 `.vault` 文件显示：文件名、文件大小、加密时间（若元数据中包含）
- [ ] 若当前目录无 `.vault` 文件，输出友好提示信息
- [ ] 支持 `--recursive` 参数递归扫描子目录（标记为 v1.1 候选，v1 不实现）

---

#### US-05：口令安全输入

作为**用户**，我希望口令输入时终端不回显字符，以便防止口令被旁观者窃取。

**验收标准**：
- [ ] 使用 `getpass` 或等效机制从终端安全读取口令，不回显任何字符
- [ ] lock 和 unlock 命令均需确认口令（lock 时输入两次，unlock 时输入一次）
- [ ] lock 时两次口令不一致，提示重新输入（最多 3 次），超过则退出
- [ ] 口令长度至少 8 个字符，不满足时提示并拒绝操作

---

## 3. 验收标准（全局）

- [ ] 所有命令在 Windows 和 Linux 上行为一致
- [ ] 完全离线可用，无网络请求
- [ ] 加密后的 `.vault` 文件无法通过口令以外的手段解密
- [ ] 所有错误场景有明确的用户可读提示和正确的退出码（0 成功，非零失败）
- [ ] Python 3.11+ 环境，仅依赖 `cryptography` 包和标准库
- [ ] 命令行通过 `argparse` 或等效库实现，提供 `--help` 帮助信息

---

## 4. 约束与依赖

### 技术约束
- **语言**：Python 3.11+
- **加密库**：`cryptography` 包（Fernet 不适用，必须使用低级 AES-256-GCM API）
- **密钥派生**：PBKDF2-SHA256，迭代次数不低于 600,000（OWASP 2023 建议）
- **CLI 框架**：标准库 `argparse`
- **无外部服务**：零网络依赖，完全离线运行
- **跨平台**：Windows / Linux

### 业务约束
- v1 版本仅处理单文件加密/解密，不处理文件夹递归
- 不做多用户或权限系统
- 不做图形界面
- 不做云存储、远程密钥管理
- 盐值存储在本地 `.vault` 目录，用户需自行保护该目录安全

### 依赖模块
- Python 标准库：`argparse`、`os`、`sys`、`getpass`、`pathlib`、`struct`
- 第三方库：`cryptography`（ hazmat primitives: ciphers/aead、kdf/pbkdf2 ）

---

## 5. 业务边界 — 本系统不做什么

| 编号 | 不做什么 | 原因 |
|:---|:---|:---|
| NB-01 | 网络传输、云存储、远程密钥管理 | 超出 v1 离线工具定位 |
| NB-02 | 文件夹递归加密 | v1 仅处理单文件，降低复杂度 |
| NB-03 | 图形界面（GUI/Web） | 项目定位为纯 CLI 工具 |
| NB-04 | 多用户/权限系统 | 个人本地工具，无多用户场景 |
| NB-05 | 密钥托管与恢复 | 用户口令是唯一解密途径，丢失口令不可恢复 |
| NB-06 | 文件内容增量加密 | v1 对整个文件进行加密，不支持差量 |
| NB-07 | 加密文件格式版本迁移 | v1 仅支持当前格式，不提供向后兼容迁移 |
| NB-08 | 嵌套加密检测 | 不检测 `.vault` 文件是否已被加密后再次加密 |

---

## 6. 相关引用

- 需求输入：`C:\tower-of-babel\projects\vault-cli\_system\user_input.md`
- 需求模板：`C:\tower-of-babel\projects\vault-cli\_shared\doc_templates\requirement.md`
- 岗位裁剪：PM、架构师、后端、测试编写、测试执行、审核员

---

## 7. .vault 文件格式规范（草案）

供架构师和后端开发参考：

```
+-------------------+------------------+-------------------+------------------+
| 版本号 (4 bytes)  | IV (12 bytes)    | Auth Tag (16 bytes)| 密文 (变长)      |
+-------------------+------------------+-------------------+------------------+
```

- 版本号：`0x01`（uint32 big-endian）
- IV：AES-GCM 推荐 12 字节
- Auth Tag：AES-GCM 16 字节
- 密文：原文件内容经 AES-256-GCM 加密后的输出

---

## 8. 里程碑与优先级

| 优先级 | User Story | 说明 |
|:---|:---|:---|
| P0 | US-01 初始化密钥库 | 其他功能的前置依赖 |
| P0 | US-02 加密文件 | 核心功能 |
| P0 | US-03 解密文件 | 核心功能 |
| P0 | US-05 口令安全输入 | 安全底线 |
| P1 | US-04 列出加密文件 | 辅助功能，可稍后交付 |
