# Puck 任务 — DT 组收尾 + ML 组启动

> 派发者：Atlas
> 时间：2026-04-23 20:26
> Tony 指示：按流程走完，不要跳步

---

## 任务一：DT 组重测（高优先级）

前 4 个插件重构后**没重跑测试**，必须补上。

| 插件 | 重构内容 | 需重测 |
|------|----------|--------|
| parse_json | _fail() + 惰性日志 | ✅ |
| dict_to_list | _fail() + 惰性日志 | ✅ |
| list_to_dict | _fail() + 惰性日志 | ✅ |
| filter_dict_list | _fail() + 惰性日志 | ✅ |

**流程**：用 triggers 重跑测试 → 更新 test_report.md → 确认全部 PASS

**代码位置**：`C:\civibbs\pipeline\done\{插件名}\`

---

## 任务二：v2.5 目录已就位

DT 组 7 个插件已复制到 `C:\civibbs\v2.5\lib\small\dt\`，包含：
- execute.py（重构后版本）
- definition.yaml
- triggers/

**v2.5 定位**：整合 v1.0 + v2.0 优秀部分，跑通 + Opus 更新的代码直接进 v2.5。

---

## 任务三：ML 组开发

9 个邮件操作插件，definition.yaml 在 `C:\civibbs\v2.0\lib\small\ml\`。

**开发规范**（必须遵守）：
1. 必须有 `_fail()` 统一错误出口
2. 日志使用惰性格式 `%s/%d`
3. definition.yaml 用 `logic.behavior` 契约
4. 代码直接进 `C:\civibbs\v2.5\lib\small\ml\`

**模范参考**：`C:\civibbs\v2.5\lib\small\dt\aggregate_results\execute.py`

**派发顺序**：
1. atomic_take（核心）
2. atomic_write（核心）
3. create_agent_mailbox
4. create_public_mailbox
5. resolve_mail_target
6. wait_for_mail_arrival
7. create_topo_storage_with_copies
8. create_multiple_topo_units
9. send_shutdown_to_topo_unit

---

## 任务四：DT 组归档

Robin 监督：
- 所有参与者写总结
- 确认 pipeline/done 目录完整

---

## 流程要求

**每个插件必须走完**：开发 → 测试 → 审核 → 并入 v2.5

**不能跳步。**

---

*Atlas 🏛️ | 2026-04-23 20:26*
